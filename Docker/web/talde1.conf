
<VirtualHost 8082:80>
        ServerAdmin webmaster@talde1.edu
        ServerName www.talde1.edu
        DocumentRoot /var/www/html
        DirectoryIndex index.html
        ErrorLog ${APACHE_LOG_DIR}/talde1-error.log
        CustomLog ${APACHE_LOG_DIR}/talde1-access.log combined

        # RewriteEngine On
        # RewriteCond %{HTTPS} !=on
        # RewriteRule ^/?(.*) https://%{SERVER_NAME}/$1 [R,L]
</VirtualHost>

# <VirtualHost *:443>
#         ServerAdmin webmaster@talde1.com
#         ServerName www.talde1.com
#         DocumentRoot /var/www/html
#         DirectoryIndex index.html
#         # <Directory /var/www/html>
#         #         AuthType Basic
#         #         AuthName "restringir"
#         #         AuthUserFile /etc/apache2/pass/.htpasswd
#         #         Require user administratzaile
#         # </Directory>
#         ErrorLog ${APACHE_LOG_DIR}/talde1-error.log
#         CustomLog ${APACHE_LOG_DIR}/talde1-access.log combined

#         SSLEngine on
#         SSLCertificateFile /etc/ssl/certs/www.talde1.com.crt
#         SSLCertificateKeyFile /etc/ssl/private/www.talde1.com.key
# </VirtualHost>